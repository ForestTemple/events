#!/bin/sh
if [ -z "$1" ] || [ -z "$2" ] || [ -z "$3" ]; then exit 1; fi
psql events -c "insert into locations (latitude, longitude, name) values ($1, $2, '$3');"
if [ "$?" -eq 0 ]; then ./locations; else exit 1; fi
